# fly.toml — WORKER MODE
# This app runs background jobs (ingestion/backfill), not HTTP services.

app = "solana-analytics"
primary_region = "ewr"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1

[http_service]
  internal_port = 8000
  force_https = true


